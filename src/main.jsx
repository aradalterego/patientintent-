import React from "react";
import ReactDOM from "react-dom/client";
import FaceAreaSelector from "./FaceAreaSelector.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FaceAreaSelector />
  </React.StrictMode>
);
